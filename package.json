{
  "name": "sam-gov-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Remote MCP server that exposes the SAM.gov Get Opportunities Public API as tools for AI agents, with NAICS-code and response-deadline filtering.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "agents": "0.20.1",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20250101.0"
  }
}
